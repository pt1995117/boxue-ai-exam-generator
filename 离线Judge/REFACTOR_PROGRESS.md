# 重构进度

## A 批次：骨架拆分（已完成）
- [x] 新增 `state.py`，抽离状态定义与初始化。
- [x] 新增 `routes.py`，抽离路由逻辑。
- [x] 新增 `builder.py`，抽离图构建逻辑。
- [x] 新增 `runner.py`，抽离运行与兜底逻辑。
- [x] `graph.py` 改为兼容门面导出。

## B 批次：节点拆分（进行中）
- [ ] 将 `graph.py` 内节点按层迁移到 `nodes/` 目录。
- [ ] 保持 `builder.py` 只依赖 `nodes/*`。

## C 批次：清理与收口
- [ ] 清理 `graph.py` 冗余实现，保留最小 facade。
- [ ] 同步文档与流程图。
- [ ] 再次执行全量测试与入口冒烟验证。
