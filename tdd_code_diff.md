# 技术设计文档 vs 实际代码差异分析

## 概述

本文档对比了 `tdd_spec.md`（技术设计文档）和实际代码实现之间的差异。主要检查了关键功能点的实现情况。

**最后更新**: 2025-01-27  
**检查范围**: 核心功能、节点实现、约束条件、用户交互、过程可视化

---

## 1. 母题检索策略（FR2.1.1 - 五级阶梯关联策略）

### 📋 技术文档要求

技术文档详细描述了五级阶梯关联策略：
1. **策略 1：反向索引复用 (Reverse Index - P0)** - 置信度 1.0
2. **策略 2：GPS 路径坐标对齐 (Path-Based GPS Match - P1)** - 置信度 0.95/0.90/0.85
3. **策略 3：法条与编码硬碰撞 (Statute Collision - P2)** - 置信度 0.88
4. **策略 4：语义向量检索 (BGE Vector Retrieval - P3)** - 使用 BGE 模型
5. **策略 5：LLM 专家逻辑重排序 (LLM Reranking - P4)** - 置信度 0.80

### 💻 实际代码实现

**文件**: 
- `map_knowledge_to_questions.py`: 实现了完整的五级阶梯关联策略（用于生成映射文件）
- `exam_factory.py` - `get_examples_by_knowledge_point()` 方法（第199-287行）: 运行时检索逻辑

**实际实现**:

**`map_knowledge_to_questions.py`（映射文件生成）**:
- ✅ **策略 1（已实现）**: 反向索引复用（第778-797行）
- ✅ **策略 2（已实现）**: GPS 路径坐标对齐（第799-871行），包括全路径包含、末端节点对齐、部分路径匹配
- ✅ **策略 3（已实现）**: 法条与编码硬碰撞（代码中有相关逻辑）
- ✅ **策略 4（已实现）**: BGE 语义向量检索（第907-950行），使用 `BAAI/bge-small-zh-v1.5` 模型
- ✅ **策略 5（已实现）**: LLM 专家逻辑重排序（第951-990行）

**`exam_factory.py`（运行时检索）**:
- ✅ **策略 1（已实现）**: 使用 `self.kb_to_questions` 反向索引（第251-284行）
- ✅ **策略 2（已实现）**: 通过篇/章/节列进行路径匹配（第203-248行）
- ⚠️ **策略 3-5（通过映射文件间接实现）**: 运行时直接使用预生成的映射文件，不再重复执行策略3-5

**差异总结**:
- ✅ 五级阶梯策略在 `map_knowledge_to_questions.py` 中完整实现
- ✅ 运行时检索使用预生成的映射文件，这是合理的优化（避免每次运行时都执行BGE和LLM）
- ⚠️ 标准化"脱水"预处理可能在 `map_knowledge_to_questions.py` 中实现，需要确认
- ✅ 映射文件格式：实际文件使用 `matched_questions` 数组格式，符合文档要求

**结论**: 
- ✅ 五级阶梯关联策略已完整实现，分为两个阶段：
  1. **离线阶段**（`map_knowledge_to_questions.py`）：生成映射文件，执行完整五级策略
  2. **运行时阶段**（`exam_factory.py`）：使用预生成的映射文件，快速检索

---

## 2. Router 节点实现

### 📋 技术文档要求

- 特征检测：公式、表格、列表
- 路由决策：计算类 → CalculatorAgent，法律类 → LegalAgent，综合类 → GeneralAgent
- 题型推荐：公式→单选，列表→多选，表格→判断
- 重路由检测和状态清理

### 💻 实际代码实现

**文件**: `exam_graph.py` - `router_node()` 函数（第498行开始）

**实际实现**:
- ✅ **特征检测**: 实现了公式、表格、列表检测（第508-520行）
- ✅ **路由决策**: 实现了 LLM 分析 + 特征强制修正（第574-598行）
- ✅ **题型推荐**: 实现了推荐逻辑（第521-530行），并传递到 `router_details.recommended_type`
- ✅ **重路由检测**: 实现了状态清理（第617-628行）

**差异总结**:
- ✅ 基本符合技术文档要求
- ⚠️ 代码中 `FinanceAgent` 会被强制转换为 `CalculatorAgent`（第598行），符合文档说明

---

## 3. 难度范围约束传递与验证

### 📋 技术文档要求

- 所有节点（Specialist、Calculator、Writer、Fixer、Critic）都需要验证和调整难度值
- Writer 节点：验证并调整难度值到指定范围
- Fixer 节点：验证并调整难度值到指定范围
- Critic 节点：验证难度值是否符合范围

### 💻 实际代码实现

**Specialist Node** (`exam_graph.py` 第630行):
- ✅ 在提示词中包含难度要求（第836-852行）
- ✅ **符合要求**：生成节点通过提示词约束难度，由 Writer 节点统一验证和调整

**Calculator Node** (`exam_graph.py`):
- ✅ 在提示词中包含难度要求
- ✅ **符合要求**：生成节点通过提示词约束难度，由 Writer 节点统一验证和调整

**Writer Node** (`exam_graph.py` 第927行):
- ✅ 在提示词中包含难度要求（第1005-1018行）
- ✅ **实现了难度值验证和调整**（第1141-1147行）：如果不在范围内，调整到范围中点

**Fixer Node** (`exam_graph.py` 第1817行):
- ✅ 在提示词中包含难度要求（第1869-1884行）
- ✅ **实现了难度值验证和调整**（第2012-2017行）：如果不在范围内，调整到范围中点

**Critic Node** (`exam_graph.py` 第1200行):
- ✅ **实现了难度值验证逻辑**（第1726-1736行）：如果不在范围内，标记为严重问题（major issue）

**差异总结**:
- ✅ **所有节点都已实现难度约束**：
  - 生成节点（Specialist/Calculator）：通过提示词约束
  - Writer 节点：验证并调整难度值
  - Critic 节点：验证难度值，不符合则标记为严重问题
  - Fixer 节点：验证并调整难度值

**建议**: 
- 在 Writer 节点解析 JSON 后，添加难度值验证和调整逻辑
- 在 Critic 节点添加难度值验证，如果不符合范围应标记为问题

---

## 4. 计算器参数提取规则（FR4.6）

### 📋 技术文档要求

- 必须提取具体数值（如80平方米、1560元、2025年）
- 禁止描述性文字（如"成本价"、"建筑面积"）
- 参数类型验证和单位统一
- 参数重构：修改原文案例数值
- 业务逻辑保持和常量保留

### 💻 实际代码实现

**文件**: `exam_graph.py` - `calculator_node()` 和 `critic_node()`

**实际实现**:
- ✅ 在 `CALCULATION_GUIDE` 中说明了参数提取规则（第39行）
- ✅ Critic 节点会动态生成代码并提取参数（第1300-1457行）
- ✅ **参数重构逻辑在生成节点的提示词中说明**（Specialist/Calculator节点），由LLM执行
- ⚠️ **代码层面缺少参数重构的自动化验证**（依赖LLM理解和Critic验证）

**差异总结**:
- 参数提取规则主要在提示词中说明，代码层面缺少强制验证
- 缺少参数重构的自动化实现

---

## 5. 题目质量硬性约束（FR5.7）

### 📋 技术文档要求

- 禁止模糊用语
- 选项维度一致性
- 禁止定义题
- 唯一正确性
- 数据重构
- 禁止同义反复/信息泄露（>60%重合度）
- 禁止AI幻觉/非人话
- 禁止"最XX"考法
- 唯一答案强制校验

### 💻 实际代码实现

**文件**: `exam_graph.py` - `critic_node()` 函数（第1459-1581行）

**实际实现**:
- ✅ **地理与范围审计**: 已实现（第1489-1496行）
- ✅ **逻辑自洽性审计**: 已实现（第1498-1503行）
- ✅ **反向解题验证**: 已实现（第1504-1523行）
- ✅ **质量把关（完整实现）**: 
  - ✅ **题干直接给出答案检测**（第1526-1530行）：明确检测题干是否直接包含答案关键词，**并说明允许正确答案选项与教材原文定义一致**
  - ✅ 基础质量检查（第1531-1534行）：模糊词汇、选项跨维度、干扰项过于幼稚
  - ✅ AI幻觉/非人话检测（第1535-1538行）
- ✅ **选项逻辑审计**: 已实现（第1540-1543行）
- ✅ **质量检查结果处理**（第1626-1637行）：通过 `quality_check_passed` 和 `quality_issues` 处理质量问题

**差异总结**:
- ✅ Critic 节点完整实现了质量约束检查
- ✅ **题干直接给出答案检测已实现**，且明确区分了"题干直接给出答案"（禁止）和"答案选项与教材原文一致"（允许）
- ✅ 质量检查结果会通过 `quality_check_passed` 和 `quality_issues` 影响最终判定
- ⚠️ 部分约束（如禁止"最XX"考法、唯一答案强制校验）在提示词中说明，由LLM执行

---

## 6. 地理和时间逻辑约束（FR5.8, FR5.9）

### 📋 技术文档要求

**地理一致性**:
- 地理继承
- 严禁无关城市
- 母题城市替换
- 干扰项特例
- 通用规则处理

**时间逻辑**:
- 时间继承
- 时间使用规则
- 时间干扰项
- 计算时间精度

### 💻 实际代码实现

**Writer Node** (`exam_graph.py` 第1042-1054行):
- ✅ 在提示词中明确说明了地理和时间约束

**Critic Node** (`exam_graph.py` 第1489-1496行):
- ✅ 实现了地理与范围审计

**差异总结**:
- ✅ 约束在提示词和 Critic 验证中都有体现
- ⚠️ 缺少代码层面的自动化检查和修正（主要依赖 LLM 理解和 Critic 验证）

---

## 7. 掌握程度约束（FR7.5）

### 📋 技术文档要求

- 从知识点切片读取掌握程度
- 在提示词中说明掌握程度要求
- 影响题目复杂度设计

### 💻 实际代码实现

**Router Node** (`exam_graph.py` 第498行):
- ✅ 提取掌握程度（第511行）
- ✅ 传递到 `router_details`（第606行）

**Calculator Node** (`exam_graph.py` 第2147行):
- ✅ **在提示词中使用了掌握程度**（第2180行、第2342行）
- ✅ 格式：`当前知识点的掌握程度要求为: 【{mastery}】。`

**Specialist Node** (`exam_graph.py` 第630行):
- ❌ **未在提示词中使用掌握程度信息**
- 第855行开始的prompt中没有包含mastery变量
- Router节点已提取掌握程度（第511行），但Specialist节点未使用

**差异总结**:
- ✅ Router节点提取了掌握程度
- ✅ Calculator节点在提示词中使用了掌握程度
- ❌ **Specialist节点未在提示词中使用掌握程度**
- **影响**：Specialist节点生成的题目可能未充分考虑掌握程度要求（了解/熟悉/掌握）对题目复杂度的影响

---

## 8. 数据模型差异

### 📋 技术文档要求

**知识点映射数据模型** (`question_knowledge_mapping.json`):
```json
{
  "0": {
    "完整路径": "...",
    "掌握程度": "掌握",
    "matched_questions": [...],
    "total_matches": 2,
    "methods_used": ["GPS_Path", "LLM_Logic"]
  }
}
```

### 💻 实际代码实现

**文件**: `exam_factory.py` 第100-117行

**实际使用**:
- 代码期望的格式：`matched_kb_path`, `confidence`, `method`
- 代码构建了反向索引 `kb_to_questions`，键为 `kb_path`，值为题目索引列表

**差异总结**:
- ⚠️ 映射文件格式可能不匹配
- 代码期望的是 `question → kb` 映射，但文档描述的是 `kb → questions` 映射
- 需要确认实际映射文件的格式

---

## 9. 计算器函数列表

### 📋 技术文档要求

17种房地产专业计算函数，包括：
- 房龄计算（支持两种模式）
- 增值税计算（税率5.3%）

### 💻 实际代码实现

**文件**: `calculation_logic.py`

**实际实现**:
- ✅ 实现了 `calculate_vat()`，税率 5.3%（第37行）
- ✅ 实现了 `calculate_house_age()`，支持两种模式（第148行）
- ✅ 实现了多种计算函数

**差异总结**:
- ✅ 基本符合要求
- 需要确认是否所有17种函数都已实现

---

## 10. 用户交互需求（F8-F10）

### 📋 技术文档要求

- UI 配置（API配置、代理设置、章节选择、出题参数设置）
- 过程可视化（实时展示各节点处理过程）
- 结果导出（题目展示、Excel导出）

### 💻 实际代码实现

**文件**: `app.py`

**需要检查**:
- UI 配置实现情况
- 流式输出和实时展示
- Excel 导出功能

**差异总结**:
- 需要进一步检查 `app.py` 的实现细节

---

## 总结

### ✅ 已实现的功能

1. Router 节点的基本路由逻辑和题型推荐
2. Critic 节点的质量验证（反向解题、地理审计、质量检查等）
3. Writer、Critic、Fixer 节点的难度值验证和调整
4. 计算器函数的基本实现
5. 地理和时间约束在提示词和验证中的体现
6. 题干直接给出答案检测（已区分禁止项和允许项）
7. 五级阶梯关联策略（分离线生成和运行时检索两阶段）
8. 教材原题优先机制（builtin_examples优先，retrieved_examples补充）
9. 选项前缀清洗（去除A./A、/A:等）
10. UI功能（章节选择、掌握程度筛选、批量生成模式）
11. 智能决策机制（critical_decision函数）
12. 重路由状态清理

### ⚠️ 部分实现或需要改进的功能

1. **参数提取规则**: 主要在提示词中说明，缺少代码层面的强制验证和自动化重构
2. **掌握程度约束在Specialist节点中的使用**: 
   - ✅ Router节点提取了掌握程度（第511行）
   - ✅ Calculator节点在提示词中使用了掌握程度（第2180行、第2342行）
   - ❌ **Specialist节点未在提示词中使用掌握程度**（第855行开始的prompt中没有包含mastery信息）
   - 技术文档要求（FR7.5）：掌握程度应在提示词中说明，影响题目复杂度设计
   - **影响**：Specialist节点生成的题目可能未充分考虑掌握程度要求（了解/熟悉/掌握）对题目复杂度的影响
4. **掌握程度约束**: Router 提取了，但生成节点可能未充分利用

### ❌ 未实现或缺失的功能

1. **参数提取和重构的代码层面自动化验证**（目前依赖LLM理解和Critic验证）
2. **Specialist节点未使用掌握程度约束**：
   - Router节点提取了掌握程度（第511行）
   - Calculator节点在提示词中使用了掌握程度（第2180行、第2342行）
   - **Specialist节点未在提示词中包含掌握程度信息**（第855行开始的prompt中没有包含mastery变量）
   - 技术文档要求（FR7.5）：掌握程度应在提示词中说明，影响题目复杂度设计
   - **影响**：Specialist节点生成的题目可能未充分考虑掌握程度要求（了解/熟悉/掌握）对题目复杂度的影响

### ⚠️ 部分实现的功能

1. **重路由过程展示**（FR9.9）：
   - ✅ 代码中实现了重路由状态清理和日志记录（第617-626行）
   - ✅ UI中显示了retry_count（第410-411行，通过`round_label`显示）
   - ✅ UI中显示了critic_details（第430行），包含驳回原因
   - ⚠️ **UI中可能缺少明确标识"这是重路由"的提示**（虽然有retry_count和details，但可能不够直观）
   - ⚠️ **UI中可能缺少问题类型（严重/轻微）的明确展示**（虽然critic_result中有issue_type，但UI中可能未明确展示）

2. **Critic评审过程展示**（FR9.6）：
   - ✅ 显示评审结果（通过/不通过）（第427-430行）
   - ✅ 显示评审原因（第430行，通过details展示）
   - ⚠️ **问题类型（严重/轻微）可能未在UI中明确展示**（critic_result中有issue_type字段，但UI中可能未显示）
   - ⚠️ **修复策略（fix_strategy）可能未在UI中明确展示**（critic_result中有fix_strategy字段，但UI中可能未显示）

3. **Fixer修复过程展示**（FR9.7）：
   - ✅ 显示修复后的题目（第434-437行）
   - ⚠️ **修复策略（fix_strategy）可能未在UI中明确展示**（虽然Fixer节点接收了fix_strategy，但UI中可能未明确展示）

---

## 建议

1. **优先级 P0**（已完成）:
   - ✅ Writer 节点难度值验证和调整 - **已实现**
   - ✅ Critic 节点难度值验证 - **已实现**
   - ✅ 映射文件格式 - **已确认符合要求**

2. **优先级 P1**:
   - ✅ 五级阶梯关联策略 - **已在 `map_knowledge_to_questions.py` 中完整实现**
   - ⚠️ 参数提取和重构的代码层面验证（可选，目前依赖LLM和Critic验证）
   - ⚠️ **Specialist节点添加掌握程度约束**（Calculator节点已实现，Specialist节点需要补充）
   - ⚠️ **UI中明确展示问题类型（严重/轻微）**（critic_result中有issue_type，但UI中可能未明确展示）

3. **优先级 P2**:
   - ⚠️ 增强数据重构的自动化实现（可选优化）

---

**文档创建时间**: 2025-01-27  
**对比版本**: tdd_spec.md v1.0 vs 实际代码
