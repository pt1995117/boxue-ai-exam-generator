# 信息不对称校验和修改指令实施清单

## 已完成的修改

### ✅ 1. Router Node - 计算分判定
- [x] 将 "金融分" 改为 "计算分" (`score_calculation`)
- [x] 添加 `need_calculation` 字段
- [x] 更新路由逻辑，明确判断是否需要生成计算题
- [x] 将 `FinanceAgent` 改为 `CalculatorAgent`

### ✅ 2. Calculator Node - 节点重命名
- [x] 重命名 `finance_node` → `calculator_node`
- [x] 更新提示词中的角色名称

## 待完成的修改

### ⏳ 3. Writer Node - "教材为实，母题为样"

**位置**: `exam_graph.py` 第 605 行

**需要修改的约束部分**:
```python
约束: 
1. "题干"中**禁止**出现"根据材料"、"依据参考资料"等字眼
2. 题目应体现对房地产经纪业务专业性、合规要求、客户服务或计算准确性的考察，避免纯记忆性条款或投诉级别数量
3. 禁止用"最重要/最关键/重点/主要"等表述设计题干或选项，优先考察完整流程、条件、责任边界或操作要点
4. **严禁照搬母题中的数值、条件或逻辑**：必须根据教材规则重新设计题目，使用新的数值和场景
5. **必须包含教材中的所有判定条件**：不得遗漏任何条件（如"容积率 > 1.0"、"房龄+贷款年限≤50年"等），确保题目完整还原教材规则
6. 选项文本内**不要写 A./B./C./D. 前缀**，只保留选项内容本身
{writer_uniqueness}
```

**已添加**:
- ✅ 核心原则说明（第 588-590 行）
- ✅ 母题参考文本（第 576-581 行）

### ⏳ 4. Calculator Node - 动态代码生成

**位置**: `exam_graph.py` 第 1272-1300 行

**需要修改**:
- 修改 prompt_gen，要求生成 Python 代码
- 解析响应中的 `python_code` 和 `question_data`
- 将 `python_code` 存储到 state 的 `generated_code` 字段

### ⏳ 5. Critic Node - 信息不对称校验

**位置**: `exam_graph.py` critic_node 函数

**需要添加的检查**:
1. **Grounding Check**: 检查题目是否遗漏了教材中的判定条件
2. **母题冲突检查**: 检查是否误带入了母题中的陈旧逻辑或错误数据

## 实施步骤

1. 手动修改 writer_node 的约束部分（第 605 行）
2. 手动修改 calculator_node 的代码生成部分（第 1272-1300 行）
3. 在 critic_node 中添加信息不对称校验逻辑
