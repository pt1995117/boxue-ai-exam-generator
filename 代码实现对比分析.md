# 代码实现与PRD FR2.1.1方案对比分析

## 一、预处理：标准化"脱水" (Normalization)

### PRD要求：
1. **关键词清洗**：移除"第X篇"、"第X章"、"第X节"、"（了解/掌握/熟悉）"、"-无需修改"等描述性词汇
2. **符号归一化**：将所有全角标点、空格、斜杠统一转换为标准分隔符 `/`
3. **同义词映射**：建立字典，如 `个税` ↔ `个人所得税`，`贝壳` ↔ `贝壳找房/BEIKE`

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第48-55行

```python
def normalize_text(text):
    """Normalize text for comparison."""
    if not isinstance(text, str):
        return ""
    # Remove punctuation and whitespace
    text = re.sub(r'[^\w\s]', '', text)
    text = re.sub(r'\s+', ' ', text)
    return text.strip().lower()
```

### ❌ 不符合项：
1. **缺少关键词清洗**：没有移除"第X篇"、"第X章"、"第X节"、"（了解/掌握/熟悉）"、"-无需修改"等
2. **缺少符号归一化**：没有统一转换为标准分隔符 `/`
3. **缺少同义词映射**：没有建立同义词字典

---

## 二、策略 1：反向索引复用 (Reverse Index - P0)

### PRD要求：
- **逻辑**：直接碰撞现有的 `question_knowledge_mapping.json`
- **置信度**：1.0
- **优先级**：最高优先级，如果匹配到则立即返回

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第628-645行

```python
# Strategy 1: Use reverse index from existing mapping (PRD requirement)
if kb_idx in reverse_index:
    for q_idx in reverse_index[kb_idx]:
        matched_set.add(q_idx)
        matched_questions.append({
            'question_index': int(q_idx),
            'confidence': 1.0,
            'method': 'exact_path_match',
            'evidence': {'source': 'existing_mapping', 'kb_path': kb_path}
        })
    if matched_questions:
        methods_used.append('exact_path_match')

# If we have matches from reverse index, return early (PRD: priority)
if matched_questions:
    matched_questions.sort(key=lambda x: x['confidence'], reverse=True)
    return matched_questions, methods_used, [], []
```

### ✅ 符合项：
- 实现了反向索引匹配
- 置信度设置为1.0
- 匹配到后立即返回

---

## 三、策略 2：GPS 路径坐标对齐 (Path-Based GPS Match - P1)

### PRD要求：
- **全路径包含**：若母题的"章+节+考点"完全包含在 KB 的路径中，置信度 0.95
- **末端节点对齐**：若母题的"考点"字段与 KB 的"切片标题"完全一致，置信度 0.90
- **部分路径匹配**：若母题的"章+节"在 KB 路径中，置信度 0.85

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第647-661行

```python
# Strategy 2: Exact kaodian match (考点精确匹配)
# Check if question's "考点" field exactly matches knowledge slice path
for kaodian_clean, q_indices in kaodian_index.items():
    if kaodian_clean and kaodian_clean in kb_path:
        for q_idx in q_indices:
            if q_idx not in matched_set:
                matched_set.add(q_idx)
                matched_questions.append({
                    'question_index': int(q_idx),
                    'confidence': 0.85,
                    'method': 'exact_path_match',
                    'evidence': {'kaodian': kaodian_clean, 'kb_path': kb_path}
                })
```

### ❌ 不符合项：
1. **缺少GPS路径坐标构建**：没有构建"标准化(篇/章/节/考点)"的GPS坐标
2. **缺少全路径包含检查**：没有检查"章+节+考点"是否完全包含在KB路径中
3. **缺少末端节点对齐**：没有检查考点与切片标题的一致性
4. **缺少部分路径匹配**：没有检查"章+节"的匹配
5. **置信度单一**：只有0.85，没有根据匹配程度区分0.95/0.90/0.85
6. **缺少标准化处理**：没有对路径进行标准化"脱水"处理

---

## 四、策略 3：法条与编码硬碰撞 (Statute Collision - P2)

### PRD要求：
- **逻辑**：正则表达式提取题干或解析中的法条编号（如《民法典》第215条）
- **匹配**：若知识切片标题或正文中出现相同法条编号，置信度 0.88
- **适用场景**：法律条文相关的题目和知识切片

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第75-82行

```python
def extract_legal_references(text):
    """Extract legal references like 《民法典》第XX条 from text."""
    if not isinstance(text, str):
        return []
    # Pattern: 《法律名称》第XX条 or 《法律名称》第XX条、第YY条
    pattern = r'《([^》]+)》第(\d+)条'
    matches = re.findall(pattern, text)
    return matches  # Returns list of (law_name, article_number) tuples
```

### ❌ 不符合项：
1. **函数存在但未使用**：`extract_legal_references` 函数已定义，但在 `find_matching_questions` 中**完全没有调用**
2. **缺少匹配逻辑**：没有实现法条编号的匹配逻辑
3. **缺少置信度设置**：没有设置0.88的置信度

---

## 五、策略 4：语义向量检索 (BGE Vector Retrieval - P3)

### PRD要求：
- **模型**：`BAAI/bge-small-zh-v1.5` ✅
- **输入构建**：
  - **母题输入** = `[标准化路径]` + `[题干]` + `[选项拼合]`
  - **知识输入** = `[完整路径]` + `[核心内容摘要]`
- **评分逻辑**：
  - `Score > 0.75`：自动通过，置信度 = Score
  - `0.5 < Score <= 0.75`：进入策略 5（逻辑复核），置信度 = Score
  - `Score <= 0.5`：不匹配

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第663-693行

```python
# Strategy 3: BGE Semantic Vector Retrieval (Fallback mechanism)
# Only use BGE if no matches found from reverse index or exact match
if not matched_questions:
    kb_embedding_text = get_kb_content_for_embedding(kb_entry)
    
    # Build candidate pool from all questions (since no prior matches)
    candidate_pool = list(range(min(TOP_K_RETRIEVAL, len(questions_df))))
    
    # BGE semantic matching
    for q_idx in candidate_pool:
        row = questions_df.iloc[q_idx]
        q_embedding_text = get_question_content_for_embedding(row)
        
        # BGE semantic matching
        bge_score = compute_bge_similarity(kb_embedding_text, q_embedding_text)
        
        # Use BGE as fallback: accept matches with score > threshold
        if bge_score > BGE_FALLBACK_THRESHOLD:
            matched_questions.append({
                'question_index': int(q_idx),
                'confidence': round(bge_score, 3),
                'method': 'bge_semantic_match',
                'evidence': {
                    'bge_score': round(bge_score, 3),
                    'note': 'BGE semantic vector retrieval (fallback)'
                }
            })
```

### ⚠️ 部分符合项：
1. **模型正确**：使用了 `BAAI/bge-small-zh-v1.5` ✅
2. **输入构建**：需要检查 `get_kb_content_for_embedding` 和 `get_question_content_for_embedding` 是否符合要求
3. **评分逻辑不完整**：
   - ✅ 有阈值检查（BGE_FALLBACK_THRESHOLD = 0.5）
   - ❌ **缺少0.75的分层处理**：没有区分 `Score > 0.75`（自动通过）和 `0.5 < Score <= 0.75`（进入策略5）
   - ❌ **没有调用策略5**：即使Score在0.5-0.75之间，也没有进入LLM重排序

---

## 六、策略 5：LLM 专家逻辑重排序 (LLM Reranking - P4)

### PRD要求：
- **触发条件**：策略 4 筛选出的前 3-5 个候选切片（Score > 0.5 且 <= 0.75）
- **Prompt 逻辑**：使用指定的专家Prompt
- **输出格式**：JSON，包含 `is_related` (bool) 和 `reason` (string)
- **置信度**：根据 LLM 的 `is_related` 结果设为 0.80（如果相关）

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第173-309行（`llm_semantic_match`函数）

### ❌ 不符合项：
1. **函数存在但未使用**：`llm_semantic_match` 函数已定义，但在 `find_matching_questions` 中**完全没有调用**
2. **Prompt不符合要求**：当前的Prompt与PRD要求的Prompt不一致
3. **缺少触发逻辑**：没有实现"Score > 0.5 且 <= 0.75"时触发LLM重排序的逻辑
4. **缺少置信度设置**：没有设置0.80的置信度

---

## 七、针对"1:N 映射"的特殊处理逻辑

### PRD要求：
- **父节点聚合**：如果策略 2 匹配到了某个"节"层级，系统会自动将该"节"下所有的原子切片（Slice）列为候选
- **置信度衰减**：在 JSON 输出中，首位映射置信度最高，随后的关联项按相关度递减

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第694-697行

```python
# Sort matched questions by confidence (descending) for multi-mapping support
matched_questions.sort(key=lambda x: x['confidence'], reverse=True)
```

### ⚠️ 部分符合项：
1. **有排序**：按置信度降序排列 ✅
2. **缺少父节点聚合**：没有实现"节"层级的父节点聚合逻辑
3. **缺少置信度衰减**：没有实现置信度衰减机制

---

## 八、输出格式

### PRD要求：
```json
{
  "0": {
    "完整路径": "第一篇 > 第一章 > ...",
    "掌握程度": "掌握",
    "matched_questions": [
      {
        "question_index": 3,
        "confidence": 0.95,
        "method": "GPS_Path",
        "evidence": {
          "reason": "路径完全匹配：第四篇/第四章/第二节/个人所得税计算"
        }
      }
    ],
    "total_matches": 2,
    "methods_used": ["GPS_Path", "LLM_Logic"]
  }
}
```

### 当前代码实现：
**位置**：`map_knowledge_to_questions.py` 第734-749行

### ⚠️ 部分符合项：
1. **基本结构符合**：有 `完整路径`、`掌握程度`、`matched_questions`、`total_matches`、`methods_used` ✅
2. **method名称不符合**：当前使用 `exact_path_match`、`bge_semantic_match`，PRD要求 `GPS_Path`、`LLM_Logic` 等
3. **evidence格式不符合**：当前evidence结构简单，PRD要求包含 `reason` 字段

---

## 总结

### ✅ 已实现：
1. 策略1：反向索引复用（完全符合）
2. 策略4：BGE语义向量检索（部分符合，缺少分层处理）

### ❌ 未实现或不符合：
1. **预处理：标准化"脱水"** - 缺少关键词清洗、符号归一化、同义词映射
2. **策略2：GPS路径坐标对齐** - 实现不完整，缺少GPS坐标构建和多层次匹配
3. **策略3：法条与编码硬碰撞** - 函数存在但未使用
4. **策略5：LLM专家逻辑重排序** - 函数存在但未使用，缺少触发逻辑
5. **1:N映射特殊处理** - 缺少父节点聚合和置信度衰减
6. **输出格式** - method名称和evidence格式不符合PRD要求

### 建议：
1. 实现完整的标准化"脱水"函数
2. 实现GPS路径坐标对齐的完整逻辑
3. 在 `find_matching_questions` 中调用法条匹配和LLM重排序
4. 实现BGE评分分层处理（0.75阈值）
5. 实现父节点聚合和置信度衰减
6. 更新method名称和evidence格式以符合PRD要求
